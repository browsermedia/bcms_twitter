Description:
    Installs the bcms_twitter module.

Example:
    rails generate bcms_twitter:install

    This will:
		1. Copy any migrations from the gem into the project.
		2. Add the routes to the config/routes.rb

